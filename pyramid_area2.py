# By submitting this assignment, I agree to the following:
# “Aggies do not lie, cheat, or steal, or tolerate those who do”
# “I have not given or received any unauthorized aid on this assignment”
#
# Names: MICHAEL NGUYEN
# NAME OF TEAM MEMBER 2
# NAME OF TEAM MEMBER 3
# NAME OF TEAM MEMBER 4
# Section: 536
# Assignment: LAB-2
# Date: 02 OCTOBER 2023

from math import*

side_l = float(input("Enter the side length in meters: "))
layers = int(input("Enter the number of layers: "))
scale = 1
total_area = 0
previous_base_area = 0


if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass



if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass


if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass


if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass


if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass


if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass

if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass
if scale <= layers:
    triangle = layers ** 2

    base_area = (side_l**2 * ((sqrt(3))/4) * triangle) 
    side_area = side_l * side_l * scale * 3


    total_area += side_area 
    total_area += base_area - previous_base_area

    #print(f"layer:{i} triangle:{triangle} side area:{side_area} base area:{base_area} \nprevious:{previous_base_area} area:{total_area}")
    previous_base_area = base_area
    scale += 1
elif scale > layers:
    pass


print(f"You need {total_area:.2f} m^2 of gold foil to cover the pyramid")